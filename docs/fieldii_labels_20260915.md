# Field II 带噪数据的六参数标签（2026-09-15）

工具 `tools_generate_fieldii_labels.py`，输出 `data/labels_fieldii.jsonl`，字段与 `data/labels_console.jsonl` 相同，另加 `reference_db`、`noise_floor_top_db`、`noise_floor_bottom_db`、`tissue_pixel_fraction`、`electronic_noise_db`、`attenuation_db_cm_mhz`、`sound_speed_mps`、`backend_determined`。

## 1. 数据

- 来源 `data/field_ii/full_noise`（2026-09-15 16:06 复制的 1920 个分片）。
- 只标全部档位组合齐全的体模（每个 168 片）：训练集均匀 8 个、囊肿 3 个，共 **1848 行**。正在生成的囊肿体模 20281828（72 片）跳过。
- 仿真结束后重跑即可覆盖全部 36 个体模（约 6048 行）。

## 2. 与实机同一套判据，只换设备相关的三样输入

| 输入 | 实机 | Field II |
|---|---|---|
| 噪声底 | 每个场次一个常数（按接收机计数借用） | 按生成器参数逐行解析：注入噪声 × 波束形成 Hann 权重的 Σw²（孔径随深度变大）+ 常数 −0.313 dB |
| 阶梯 | 主机实际档位 | 深度 25/30/35/42/50/60 mm，频率 4/5/6.5/8 MHz，聚焦 5–40 mm（≤ 深度） |
| 分辨率表 | 实机点靶 | 无噪声旧数据集（`data/field_ii/full`）训练集 12 个点靶体模，缓存于 `bmode_opt/fieldii_resolution_table.json` |

### 2.1 噪声底模型的验证（`tests/measure_fieldii_noise_floor.py`）

- 38 个最深 15 mm 全为噪声的 60 mm 分片：常数 −0.313 dB，p10 −0.376、p90 −0.247，体模与频率之间一致。
- 35–60 mm（孔径 78→128 阵元，模型预测底噪下降 2.1 dB）残差逐段持平；同一次仿真 42 / 50 / 60 mm 三种裁剪相差 0.04 dB。

### 2.2 Field II 分辨率表

| 显示深度 | 比较集 | 靶 | 4 MHz | 5 MHz | 6.5 MHz | 8 MHz |
|---|---|---|---|---|---|---|
| 30 mm | 25 | 49 | 1.355 | 1.046 | 0.839 | 0.759 |
| 42 mm | 32 | 76 | 1.345 | 1.052 | 0.845 | 0.758 |
| 60 mm | 32 | 89 | 1.345 | 1.056 | 0.846 | 0.753 |

各显示深度下频率越高越锐（4→8 MHz 约 −44%），没有实机基波 10/11.4 MHz 那种台阶，也没有 3% 以内的并列。所以 Field II 上「最锐的可行档」就是「最高的可行档」。

靶位由 `point_targets_mm` 给出，按体模声速换算到图上深度（真实深度 × 1540 / 声速），只在靶周围 3 mm 取背景中位数；与整幅检测法相比分数相差 0.3%，快约 2000 倍。

## 3. 2026-09-13 推迟的两处修改

| 修改 | 做法 | 作用量（`tests/verify_fieldii_labels.py` 第 3 节） |
|---|---|---|
| 组织掩膜排除噪声 | `tissue.fieldii_tissue_mask(..., floor_db=逐行底噪)`，低于底噪 + 3 dB 不算组织 | 60 mm 分片最优增益：8 MHz 中位 **+7.06 dB**、最大 9.15 dB；6.5 MHz 中位 +1.62 dB、最大 9.16 dB；组织像素比例 8 MHz 由 1.00 降到 0.70 |
| 散斑宽度噪声过滤 | Field II 用 0.75 线（`front_end.FIELDII_SPECKLE_NOISE_WIDTH_LINES`） | 纯噪声带宽 0.50 线（最大 0.54），可用组织带最窄 0.90 线；实机的 1.5 线会丢掉 24.6% 的组织带 |

后端曝光参考 `reference_db` 改为组织像素的 dB 中位数，并写进标签；渲染起点图像时必须用同一个值。

## 4. 结果

| 轴 | 定出 | 方向分布 | 最优值分布 |
|---|---|---|---|
| 增益 | 1848 | 偏暗 1004 / 正确 52 / 偏亮 792 | 起点随机抽取 |
| TGC 近 / 中 / 远 | 1848 | 偏低 739/866/1004，正确 146/91/185，偏高 963/891/659 | — |
| 深度 | 1848 | 太浅 951 / 正确 897 / **太深 0** | 25:60, 30:112, 35:307, 42:145, 50:600, 60:624 |
| 频率 | 1848 | 偏低 986 / 正确 438 / 偏高 424 | 4:80, 5:417, 6.5:627, 8:724；全部 measured，firm 1640 |
| 聚焦 | 1848 | 太浅 631 / 正确 264 / 太深 953 | 10:331, 15:717, 20:424, 25:256, 30:120 |
| 动态范围 | 0 | 无判据，训练时掩膜 | — |

### 4.1 检查

- `tests/verify_console_labels.py data/labels_fieldii.jsonl`：算术与逻辑 0 错；频率最优随显示深度下降（秩相关 −0.44）；聚焦最优随显示深度加深（+0.72）。
- `tests/verify_frontend_chain.py data/labels_fieldii.jsonl`：1848 个起点**全部停下**，无打转；1–2 步停下的占 91%，最多 4 步。第二步主要是聚焦再调一档（832 次）：聚焦在旧深度下求，深度变了之后跟着调。
- `tests/verify_fieldii_labels.py`：
  - **标签依赖图像**：只用（显示深度、当前频率、当前聚焦）猜方向，频率 73.9%（实机 95%）、深度 74.8%、聚焦 93.6%；168 个设置里频率最优值随体模不同的有 168 个，深度 136 个，聚焦 85 个。
  - **可用深度随体模物理量变化**：同一（频率、聚焦）下与衰减的秩相关 −0.35 至 −0.89，与噪声电平 −0.05 至 −0.86。

实机标签在同样的代码上重新生成（深度候选不低于聚焦、实机分辨率表按场次标定换算），422 帧所有标签字段 0 变化。

## 5. 需要注意的性质

1. **深度标签从不说「太深」。** 求解顺序是先让频率适应当前深度，而 Field II 最深 60 mm 时 4 MHz 仍覆盖得住，所以深度只会不变或加深。实机的 132 条「太深」全部来自 58.6 mm 以上连最低频也覆盖不了的帧。在「体模上取景到信号可用的最深处」这个判据下这是正确的，但模型从 Field II 学不到「减小深度」。若要让深度标签体现感兴趣结构，可改用囊肿、点靶的真值位置取景（`front_end.structure_extent_mm` 已有），这是判据的改变，需要另行决定。
2. **频率标签偏向「调高」**：当前频率 4 MHz 的帧 454/462 说偏低。起点均匀覆盖整个网格，而多数深度下 6.5/8 MHz 可行，属于网格分布的结果，不是错误；训练时按类别加权。
3. **增益「正确」只占 2.8%**：Field II 的标签不确定度为 0，死区很窄。

---

## 6. 2026-09-16 重新生成（15 个完整体模）

仿真续跑到 2562 个分片后重新生成：**2520 行**，15 个完整体模（均匀 8、囊肿 7），耗时 185 秒。仍在生成的囊肿体模 20281832（42/168）跳过。

- **确定性核对**：与上一版共有的 1848 行**逐字段完全相同**，新增 672 行。
- 方向分布：增益 偏暗 1364 / 正确 71 / 偏亮 1085；深度 太浅 1263 / 正确 1257 / 太深 0；频率 偏低 1355 / 正确 606 / 偏高 559；聚焦 太浅 870 / 正确 360 / 太深 1290。
- 频率最优值 4:80、5:553、6.5:903、8:984，全部 measured，firm 2236 / borderline 284。
- 检查：算术 0 错；闭环链 2520 个起点全部停下、无打转；不看图猜频率方向 78.1%（上一版 73.9%，实机 95%）；掩膜排除噪声对 8 MHz、60 mm 的最优增益影响中位 +7.60 dB、最大 10.58 dB。

### 6.1 修正了一处检查本身的错误

`tests/verify_console_labels.py` 的「可用深度 vs 频率」在 15 个体模上报出 +0.84 并判为失败。查下来是检查写错了，不是标签错了：

1. 深度标签按「聚焦 → 频率 → 深度」求，用的是**挑出来的**频率。跨体模混比时，衰减低的体模既会被挑到更高频率、可用深度本来又更深，相关自然变正。
2. 这一节还先按 (族, 模式, 当前频率, 聚焦) 去重，键里没有显示深度，把同一比较集的六个显示深度压成一行，Field II 上留下的恰好全是 60 mm 那档。

改为在**同一个族/体模内部**、按条件频率与条件聚焦分组比较，并直接用所有行。结果：Field II 26 个组**全部**是最低频可用深度最深，有三档以上频率的 12 个组秩相关中位数 −1.00；实机基波 5 / 8 / 11.4 MHz 为 49.2 / 42.5 / 37.3 mm，谐波 4.4 / 5 / 5.3 / 5.7 MHz 为 43.0 / 42.5 / 34.5 / 32.0 mm。
